node_modules
dist
.limcode

# tests (keep local; do not track in git)
test/
jest.config.cjs
tsconfig.jest.json

# test artifacts
coverage
.jest-cache
.ts-jest
.cache

# lock files (project uses npm)
pnpm-lock.yaml

# logs / runtime artifacts
*.log
*.log.*
logs/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
tmp-*.out.log
tmp-*.err.log

# packaged extension bundles
*.vsix

# 本地修复资料目录只用于交接和排查；忽略整个目录，避免后续误把临时资料提交进仓库。
2026年5月27日-修复资料/
2026年5月28日 修复资料/

# 发布前本地整理资料：这些内容只服务内部审计、排查和临时实验，不上传到仓库。
# 为什么要加：docs/、tmp/ 和 .tmp/ 当前都是本地发布准备资料，继续显示为 untracked 会污染上传清单。
# 怎么改：在 git 层忽略这些目录，不删除文件，也不影响已跟踪源码与测试文件。
# 目的：让发布前 git status 只暴露真正要提交的代码、配置和发布文本。
docs/
.tmp/
tmp/
.kagent/
.tmp-limcode-dataflow-server.pid
测试mcp.json
vscode-panel-agent-shadow/